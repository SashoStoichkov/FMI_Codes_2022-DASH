@using C__website.Models
@model Orientation

Please add photo
<form method="post" enctype="multipart/form-data" asp-controller="Home" asp-action="NewPost">
    <div class="form-group">
        <div class="col-md-10">
            <p>Upload one or more files using this form:</p>
            <input type="file" name="files" multiple />
        </div>
    </div>
    
    <label>How many cubes?</label>
    @Html.TextBox("number")
    @Html.DropDownListFor(model => model.AR, new SelectList(Enum.GetValues(typeof(AspectRatio))), "Chose aspect ratio")
    <div class="form-group">
        <div class="col-md-10">
            <input type="submit" value="Upload" />
        </div>
    </div>
</form>